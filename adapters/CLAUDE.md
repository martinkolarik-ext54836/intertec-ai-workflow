# Shared AI Workflow Entry Point

@.ai/WORKFLOW.md

For every repository under this directory, also read the nearest
`.ai/project.md` and any repository `CLAUDE.md`. Keep project feature artifacts
inside that repository; never store them in the shared `.ai` directory.
